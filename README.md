# Zonvero MVP

Upload `index.html`, `styles.css`, and `app.js` to a GitHub repository, then import the repo into Vercel with the framework set to Other and no build command.

Zonvero is a working brand; confirm domain and trademark clearance before public launch.
